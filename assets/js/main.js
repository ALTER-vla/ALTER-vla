(() => {
  const dialog = document.querySelector("#video-dialog"); const dialogVideo = document.querySelector("#dialog-video"); const dialogTitle = document.querySelector("#dialog-title");
  function card(video) { const article = document.createElement("article"); article.className = "video-card"; const player = document.createElement("video"); player.src = video.src; player.preload = "metadata"; player.muted = true; player.defaultMuted = true; player.autoplay = true; player.loop = true; player.playsInline = true; player.controls = true; player.controlsList = "nodownload noplaybackrate"; player.disablePictureInPicture = true; player.setAttribute("oncontextmenu", "return false"); if (video.poster) player.poster = video.poster; article.append(player); const title = document.createElement("h3"); title.textContent = video.title; const subtitle = document.createElement("p"); subtitle.textContent = video.subtitle; article.append(title, subtitle); return article; }
  document.querySelector(".close-dialog").addEventListener("click", () => dialog.close()); dialog.addEventListener("close", () => { dialogVideo.pause(); dialogVideo.removeAttribute("src"); dialogVideo.load(); }); dialog.addEventListener("click", (event) => { if (event.target === dialog) dialog.close(); }); document.querySelector("#conveyor-grid").replaceChildren(...window.projectData.conveyor.map(card)); document.querySelector("#stapler-grid").replaceChildren(...window.projectData.stapler.map(card));
})();
