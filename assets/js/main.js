(() => {
  const grid = document.querySelector("#video-grid"); const dialog = document.querySelector("#video-dialog"); const dialogVideo = document.querySelector("#dialog-video"); const dialogTitle = document.querySelector("#dialog-title");
  function card(video) { const article = document.createElement("article"); article.className = "video-card"; if (video.src) { const player = document.createElement("video"); player.src = video.src; player.preload = "metadata"; player.muted = true; player.playsInline = true; if (video.poster) player.poster = video.poster; article.append(player); const button = document.createElement("button"); button.className = "play-button"; button.type = "button"; button.setAttribute("aria-label", `Play ${video.title}`); button.innerHTML = "<span aria-hidden=\"true\">&#9654;</span>"; button.addEventListener("click", () => { dialogVideo.src = video.src; dialogVideo.poster = video.poster || ""; dialogTitle.textContent = video.title; dialog.showModal(); dialogVideo.play().catch(() => {}); }); article.append(button); } else { const placeholder = document.createElement("div"); placeholder.className = "placeholder"; placeholder.innerHTML = "<span>Video coming soon</span>"; article.append(placeholder); } const title = document.createElement("h3"); title.textContent = video.title; const subtitle = document.createElement("p"); subtitle.textContent = video.subtitle; article.append(title, subtitle); return article; }
  document.querySelector(".close-dialog").addEventListener("click", () => dialog.close()); dialog.addEventListener("close", () => { dialogVideo.pause(); dialogVideo.removeAttribute("src"); dialogVideo.load(); }); dialog.addEventListener("click", (event) => { if (event.target === dialog) dialog.close(); }); grid.replaceChildren(...window.projectData.videos.map(card));
})();
